{
  "name": "grindingapp",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "git config core.hooksPath \"$(git rev-parse --show-toplevel)/app/.githooks\""
  }
}
